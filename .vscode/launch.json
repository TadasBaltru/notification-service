{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Listen for Xdebug (Docker app container)",
            "type": "php",
            "request": "launch",
            "port": 9003,
            "pathMappings": {
                "/app": "${workspaceFolder}"
            },
            "log": false
        }
    ]
}
